{
  "name": "kompro",
  "version": "1.0.0",
  "description": "Open-source, self-hosted, framework-agnostic compliance management platform for organizations.",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "ci": "npm ci --prefix backend && npm ci --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "lint": "npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "author": "David Oduse",
  "license": "AGPL-3.0"
}
